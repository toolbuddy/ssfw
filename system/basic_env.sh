#!/bin/sh
# set up the basic environment in Linux
sudo apt-get update
sudo apt-get install build-essential
sudo apt-get install linux-tools-common linux-tools-generic
sudo apt-get install astyle colordiff gnuplot
